# Состояние проекта

**Обновлено:** 2026-08-17, сессия 4

## Готово

- **Постановка.** `SPEC.md` (две части, 4 допущения, 10 критериев приёмки)
  и Часть II `AGENTS.md` заполнены в сессии 1.
- **Окружение.** Node.js v24.19.0 / npm 11.17.0; стенд
  `npx --yes http-server ./app -p 8765 -c-1` отвечает на `http://localhost:8765/`;
  `@playwright/test` 1.56 + Chromium установлены в сессии 2.
- **Приложение.** `app/index.html` написано (коммит `bc4d1f9`, роль «Автор»,
  журнала после себя не оставила).
- **Наивный прогон.** Выполнен и сохранён в `Naive/naive-run.md` (коммит `1e9f44c`).
  В сессии 2 намеренно не читался.
- **Тест-план.** `reports/test-plan.md`: 12 проверок, 9 негативных и граничных,
  плюс 3 отобранных пользователем сценария сверх плана (S-01, S-03, S-10).
- **Прогон по спеке.** Выполнен целиком: 11 проверок пройдены, TC-12 провалена.
  Результаты — `reports/test-run.md`.
- **Сценарии сверх плана.** Шесть штук двумя заходами: S-01, S-03, S-10 (без
  дефектов) и S-02, S-04, S-08 (дали BUG-02 и BUG-03). Вывод проб —
  `reports/probe-s02-s04-s08.txt`.
- **Три дефекта.**
  - BUG-01 (`bugs/bug-01-long-value-overflow.md`, средний): длинное значение
    без пробелов ломает вёрстку, страница шире окна в 4,2 раза при 1280
    и в 13,7 раза при 360.
  - BUG-02 (`bugs/bug-02-double-submit-shows-errors.md`, средний): двойной клик
    по «Отправить» при показанном подтверждении — запись создаётся, но вместо
    подтверждения показаны пять ошибок.
  - BUG-03 (`bugs/bug-03-broken-storage-blank-list.md`, низкий): повреждённая
    запись в `localStorage` роняет отрисовку, на экране ни списка, ни сообщения
    о пустом списке.
- **Автотест.** `tests/bug-01-overflow.spec.js` падает на BUG-01 на обеих ширинах,
  вывод сохранён в `reports/playwright-run.txt`.
- **Скриншоты.** 12 файлов в `reports/screenshots/` — пустое состояние, ошибки,
  успех, BUG-01 и BUG-02 на ширинах 360 и 1280.
- **Список расхождений.** `reports/discrepancies.md`: четыре ложных срабатывания
  при тестировании (по каждому — что утверждалось, чем опровергнуто, вывод),
  непроверенный сценарий S-09 как дыра в покрытии, расхождение по трактовке
  наивного прогона из сессии 1.
- **Повторная проверка после фикса BUG-01.** `reports/retest.md`: автотест
  из 2 failed стал 2 passed, `scrollWidth` 5327 → 1280 и 4943 → 360, TC-12
  пройдена. Побочного ущерба нет: 16 из 16 позиций чек-листа в ожидаемом
  состоянии, необработанных исключений ноль, пробы S-02/S-04/S-08 и проверка
  находок наивного прогона совпадают построчно с прогоном 16.08.
  **BUG-01 закрыт**, статус обновлён в баг-репорте.
- **Сравнение двух прогонов.** `reports/comparison.md`: 3 дефекта против 4,
  19 артефактов против 0, 12 проверок с зафиксированным входом против 21 пункта
  прозой; пересечение — один дефект (вёрстка на длинных значениях). Три
  утверждения наивного прогона перепроверены на стенде 8765
  (`reports/probe-naive-claims.txt`): два подтвердились, одно («Enter не
  срабатывает») оказалось артефактом его песочницы.
- **Независимый аудит.** Сессия 3, роль «Аудитор», чистый контекст:
  `reports/audit.md` — 15 находок (2 критично, 5 важно, 8 мелочь). Ничего
  не правилось. Главное: (1) при `registrations = [null]` форма не просто
  «показывает пустое место», а перестаёт работать — отправка падает
  с `TypeError` на `String(items[i].email)`, тогда как `reports/test-run.md`
  утверждает «форма работает во всех пяти случаях»; (2) у десяти проверок
  прогона 16.08 доказательство названо «вывод консоли», но на диске его нет;
  (3) строка «обрыв JSON → pageerror» в BUG-03 — след предыдущей итерации пробы.
- **Решения по находкам аудита приняты** (сессия 4, роль «Тестировщик»):
  - **А-01 принята** → заведён `reports/bugs/bug-04-null-record-breaks-submit.md`,
    серьёзность критичная, с шагами воспроизведения и доказательством.
  - **А-02 принята** → внесена в `README.md` отдельным разделом «Ограничение
    доказательной базы».
  - **Остальные 13 находок (В-01…В-05, М-01…М-08) приняты к сведению
    и оставлены без правок — не хватило времени до сдачи.** Решение владельца
    требований, записано явно в `sessions/session-4.md`, в README и здесь.
    Следствие: `reports/test-run.md` сохраняет опровергнутую строку «Форма при
    этом работает во всех пяти случаях», BUG-03 — заниженную серьёзность
    и недоказанную строку про «обрыв JSON», `reports/test-plan.md` — критерий
    S-03, противоречащий вердикту отчёта, `reports/retest.md` — вывод «16 из 16»
    на чек-листе слабее плана. Ничего из этого не переписывалось задним числом
    намеренно: опровержение лежит рядом, в BUG-04 и в `reports/audit.md`.

## В работе

Ничего. Решения по аудиту приняты и исполнены в объёме, который выбрал
владелец требований.

## Не начато

- **BUG-05 и BUG-06 не заведены** (до сессии 4 резервировались под номерами
  BUG-04 и BUG-05; номер 04 занял реально оформленный баг-репорт по находке
  аудита). Два дефекта, найденных наивным прогоном
  и подтверждённых мной на стенде 8765: сбой записи в `localStorage` проглатывается
  молча (пользователь не видит ни успеха, ни ошибки) и битые данные затираются,
  а запись без `email` рисуется как буквальное `undefined`. Доказательства сняты
  (`reports/probe-naive-claims.txt`), решение об оформлении — за владельцем
  требований.
- Расхождения по **отчёту агента-автора** приложения. Собрать не из чего:
  сессия-автор журнала не оставила (коммит `bc4d1f9`, файла сессии нет),
  сверять её «готово» не с чем. Это записано в шапке
  `reports/discrepancies.md`, а не подогнано под формулировку критерия.
- **Тринадцать находок аудита закрыты не будут** — решение принято, см. выше.
  Что осталось неисправленным, перечислено там же и в README.
- **BUG-04 не проверен на стенде** `http://localhost:8765/`: Node в окружении
  сессий 3–4 отсутствует. Доказательство снято встроенным браузером с заглушкой
  `localStorage`, ограничение названо в самом баг-репорте, там же — команда
  для проверки на нормальном стенде. Автотест на BUG-04 не писался.
- **BUG-02, BUG-03, BUG-04 не чинились.**
- Сдача: `README.md` обновлён под итог работы, репозиторий не выложен.

## Активные инструменты

| Инструмент | Статус |
|---|---|
| Claude Code, Opus 5, режим `acceptEdits` | Используется |
| Node.js v24.19.0 / npm 11.17.0 | Проверен в сессиях 1–2; **в окружении сессии 3 отсутствует** |
| `http-server` 14.1.1 через `npx` | Стенд поднимался на 8765 в сессиях 1–2; в сессии 3 поднять не удалось |
| `@playwright/test` 1.56 + Chromium 151 | Установлен там же, где Node; `node_modules/` в окружении сессии 3 нет |
| Встроенный браузер сессии | Клики и разбор DOM — работает; **скриншоты не работают**; на `data:`-URL `localStorage` заблокирован |

## Известные проблемы

- **`node` не виден в PATH уже запущенных оболочек.** Перед любыми командами Node
  выполнять строку обновления PATH из `AGENTS.md`.
- **Встроенный браузер сессии наполовину нерабочий:** `screenshot` падает
  с `the Browser pane is not displayed`, а синтетические клики по ходу сессии
  перестали доходить до страницы (`submit` не срабатывает, программный
  `btn.click()` при этом работает). Всё, что зависит от настоящей мыши
  или картинки, гонять Playwright'ом.
- **Автотест один и покрывает только BUG-01.** После фикса BUG-02 и BUG-03
  проверять придётся руками либо дописывать тесты.
- **S-09 (значение вставлено из буфера) не проверен** — следствие того, что
  поля заполнялись скриптом. Дефекты вида «форма не видит вставленное значение»
  этим способом не ловятся.
- **Срок — 17.08.2026, 18:00.**
- **`SPEC.md` и Часть II `AGENTS.md` человеком глазами не читались** (перенесено
  из сессии 1, всё ещё актуально).
- **Окружение сессии 3 — не то, в котором снимались артефакты.** Репозиторий
  лежит в `C:\Users\User\...`, а сохранённые выводы Playwright ссылаются
  на `C:\Users\Dmnz\...`; Node здесь не установлен, `node_modules/` нет.
  Следствие: в сессии 3 не проверялись вёрстка, автотест и всё, что требует
  настоящего `localStorage` и перезагрузки страницы. Это записано в шапке
  `reports/audit.md`.
- **Открыто после аудита:** BUG-03 занижен по серьёзности и содержит одну
  недоказанную строку; у десяти проверок прогона 16.08 нет артефакта на диске;
  критерий S-03 в плане противоречит вердикту отчёта; чек-лист ретеста слабее
  плана в TC-01, TC-02, TC-07, TC-08. Подробности и приоритеты — `reports/audit.md`.

## Следующий шаг

Критерии «повторная проверка после фикса» и «независимый взгляд» **закрыты** —
см. `reports/retest.md` и `reports/audit.md`. Находки аудита разобраны: две
закрыты, тринадцать оставлены сознательно.

Осталось: **выкладка репозитория**. Работа в сдаваемом виде.

Если время появится — порядок по цене ошибки: переснять вывод по проверкам
плана и положить в `reports/` (А-02, единственное, что держится на слове),
затем разрешить противоречие в критерии S-03 (В-03), затем убрать недоказанную
строку про «обрыв JSON» из BUG-03 (В-01) — новой записью, а не правкой старой. По желанию владельца
требований — оформить BUG-04 и BUG-05 на два подтверждённых дефекта
из наивного прогона и починить BUG-02 / BUG-03. Наиболее пригоден для этого BUG-01: на него готов
падающий автотест, поэтому повторную проверку после фикса можно провести
командой, а не глазами. После фикса — **ещё одна сессия «Тестировщик»**:
прогнать чек-лист повторно и записать, что починилось и что сломалось попутно.

Дальше по критериям приёмки остаются: сравнение двух прогонов (требует чтения
`Naive/naive-run.md` — отдельная работа, в роли тестировщика этой сессии файл
намеренно не открывался), список расхождений «агент сказал готово — проверка
опровергла», аудит независимой моделью и сдача с `README.md`.
