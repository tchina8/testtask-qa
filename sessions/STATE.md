# Состояние проекта

**Обновлено:** 2026-08-16, сессия 2

## Готово

- **Постановка.** `SPEC.md` (две части, 4 допущения, 10 критериев приёмки)
  и Часть II `AGENTS.md` заполнены в сессии 1.
- **Окружение.** Node.js v24.19.0 / npm 11.17.0; стенд
  `npx --yes http-server ./app -p 8765 -c-1` отвечает на `http://localhost:8765/`;
  `@playwright/test` 1.56 + Chromium установлены в сессии 2.
- **Приложение.** `app/index.html` написано (коммит `bc4d1f9`, роль «Автор»,
  журнала после себя не оставила).
- **Наивный прогон.** Выполнен и сохранён в `Naive/naive-run.md` (коммит `1e9f44c`).
  В сессии 2 намеренно не читался.
- **Тест-план.** `reports/test-plan.md`: 12 проверок, 9 негативных и граничных,
  плюс 3 отобранных пользователем сценария сверх плана (S-01, S-03, S-10).
- **Прогон по спеке.** Выполнен целиком: 11 проверок пройдены, TC-12 провалена.
  Результаты — `reports/test-run.md`.
- **Дефект.** BUG-01 (`reports/bugs/bug-01-long-value-overflow.md`): длинное
  значение без пробелов ломает вёрстку, страница шире окна в 4,2 раза при 1280
  и в 13,7 раза при 360. Доказан тремя способами — скриншот, разбор разметки,
  вывод падающего теста.
- **Автотест.** `tests/bug-01-overflow.spec.js` падает на BUG-01 на обеих ширинах,
  вывод сохранён в `reports/playwright-run.txt`.
- **Скриншоты.** 10 файлов в `reports/screenshots/` — пустое состояние, ошибки,
  успех и дефект на ширинах 360 и 1280.

## В работе

Ничего. Сессия 2 завершена в точке, где следующий шаг требует **решения
пользователя** (см. «Следующий шаг»).

## Не начато

- Повторная проверка после фикса — фикса не было.
- Сравнение двух прогонов (наивного и по спеке) — требует чтения
  `Naive/naive-run.md`, что в роли «Тестировщик» этой сессии было исключено.
- Список расхождений «агент сказал готово — проверка опровергла» по приложению.
  Материал есть: в `reports/test-run.md` записаны три случая, где проверка
  опровергла **мою** первую гипотезу; расхождения по отчёту агента-автора
  собираются при сравнении прогонов.
- Аудит независимой моделью или чистой сессией.
- Сдача: `README.md`, репозиторий.

## Активные инструменты

| Инструмент | Статус |
|---|---|
| Claude Code, Opus 5, режим `acceptEdits` | Используется |
| Node.js v24.19.0 / npm 11.17.0 | Проверен |
| `http-server` 14.1.1 через `npx` | Стенд поднят на 8765 |
| `@playwright/test` 1.56 + Chromium 151 | Установлен, тест падает как задумано |
| Встроенный браузер сессии | Клики и разбор DOM — работает; **скриншоты не работают** |

## Известные проблемы

- **`node` не виден в PATH уже запущенных оболочек.** Перед любыми командами Node
  выполнять строку обновления PATH из `AGENTS.md`.
- **Скриншоты встроенным браузером недоступны:** `screenshot` падает с
  `the Browser pane is not displayed`. Обход — Playwright,
  скрипт `tests/capture-evidence.mjs`.
- **Дефект найден один, критерий приёмки требует трёх баг-репортов.**
  Остальное поведение либо соответствует Части A, либо попадает в намеренные
  недосказанности без внутреннего противоречия. Выдуманные баги не заводились.
- **Срок — 17.08.2026, 18:00.**
- **`SPEC.md` и Часть II `AGENTS.md` человеком глазами не читались** (перенесено
  из сессии 1, всё ещё актуально).

## Следующий шаг

Решение пользователя по открытому вопросу 1 из `session-2.md`: прогонять ли
сценарии, не вошедшие в отбор (двойная отправка, повреждённое содержимое
`localStorage`, две вкладки на одном хранилище), чтобы попытаться закрыть критерий
«три баг-репорта», — или принять один найденный дефект и записать расхождение
с критерием. После этого — **новая сессия** в роли «Фиксер» для правки BUG-01
и повторный прогон чек-листа.
